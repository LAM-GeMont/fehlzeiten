query Tutorium($tutoriumId: String!) {
    tutorium(id: $tutoriumId) {
        id
        name
        tutor {
            name
        }
        students {
            id
            firstName
            lastName
        }
    }
}