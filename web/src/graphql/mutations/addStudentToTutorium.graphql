mutation AddStudentToTutorium($addStudentToTutoriumData: AddStudentToTutoriumInput!) {
    addStudentToTutorium(data: $addStudentToTutoriumData) {
        errors {
            message
            code
        }
    }
}