mutation CreateAbsence($data: AbsenceCreateInput!) {
  createAbsence(data: $data) {
    absence {
      id
      createdAt
      updatedAt
      date
      lessonIndex
      excused
    }
    errors {
      message
      code
    }
  }
}
